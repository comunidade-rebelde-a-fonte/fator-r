/**
 * Aparência de input, select e textarea: borda `line-strong` (3,44:1) sobre `panel-2`, e borda de
 * erro quando `aria-invalid`. Layout e espaçamento ficam em cada uso.
 */
export const classesCampo =
  "rounded-[4px] border border-line-strong bg-panel-2 text-fg aria-invalid:border-danger";
